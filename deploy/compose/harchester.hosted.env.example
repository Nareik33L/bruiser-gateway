# Hosted public URLs for the Compose stack (VM or Fly in front of Caddy).
# Local compose defaults stay on *.localhost — copy this file only when hosting.
#
#   docker compose -f deploy/compose/docker-compose.demo.yml --env-file \
#     deploy/compose/harchester.hosted.env.example --profile hostnames up --build

SIMTIX_PUBLIC_URL=https://tickets.bruiser-gateway.com
DEMO_CLUB_HOST=club.bruiser-gateway.com
DEMO_TICKETS_HOST=tickets.bruiser-gateway.com
DEMO_ADMIN_HOST=admin.bruiser-gateway.com
DEMO_OPPONENT=Arsenal
